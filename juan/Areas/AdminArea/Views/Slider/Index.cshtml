
@model IEnumerable<JuanSlider>
@{
    ViewData["Title"] = "Index";
    int count = 1;
}

<h1>Slider</h1>
<div class="row">

    <div class="col-lg-12 grid-margin stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Sliders</h4>
                <div class="col-lg-12">
                    <a class="btn btn-success" asp-action="Create">Create Slider</a>
                </div>
                <div class="table-responsive pt-3">
                    <table class="table table-dark">
                        <thead>
                            <tr>
                                <th>
                                    #
                                </th>
                                <th>
                                    Image
                                </th>
                                <th>
                                    Delete
                                </th>
                                <th>
                                    Update
                                </th>
                            </tr>
                        </thead>
                        <tbody>
                            @foreach (var slides in Model)
                            {
                                <tr>
                                    <td>
                                        @count
                                    </td>
                                    <td>
                                        <img src="~/assets/img/slider/@slides.Image" alt="Alternate Text" />
                                    </td>
                                    <td>
                                        <a class="btn btn-sm btn-danger" asp-action="Delete" asp-route-id="@slides.ID"><i class="mdi mdi-delete"></i></a>
                                    </td>
                                    <td>
                                        <a class="btn btn-sm btn-warning" asp-action="Update" asp-route-id="@slides.ID"><i class="mdi mdi-lead-pencil"></i></a>
                                    </td>

                                </tr>
                                count++;
                            }
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>

